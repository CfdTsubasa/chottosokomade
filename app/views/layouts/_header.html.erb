<header>
  <nav class="flex justify-around mt-5">
    <h1>
      <%= link_to "Chottosokomade!!", root_path , {class: "text-3xl font-semibold"}%>
    </h1>
    <ul class="flex items-center">
      <% if current_user.present? %>
        <li class="font-semibold">Now Login As:&ensp;<%= link_to current_user.name, user_show_path(id: current_user.id) , {class: "text-xl"}%></li>
        <li class="ml-3 font-semibold"><%= link_to "PostSpot", new_spot_path %></li>
        <li class="ml-3 font-semibold"><%= link_to "Home", root_path %></li>
        <li class="ml-3 font-semibold"><%= link_to "Logout", destroy_user_session_path %></li>
      <% else %>
        <li class="font-semibold"><%= link_to "Log in", user_session_path %></li>
      <% end %>
    </ul>
  </nav>
</header>
